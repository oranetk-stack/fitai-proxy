{
  "name": "fitai-proxy",
  "version": "1.0.0",
  "description": "Serverless proxy for FitAI: parse-image, generate-meals, chat, barcode-lookup (Vercel-ready).",
  "main": "index.js",
  "scripts": {
    "start": "vercel dev",
    "test": "node -e \"console.log('No tests; run functions with vercel dev')\""
  },
  "dependencies": {
    "@sentry/node": "^10.9.0",
    "@upstash/redis": "^1.35.3",
    "p-limit": "^7.1.1"
  },
  "engines": {
    "node": "22.x"
  }
}
